package com.aiconsultant.consultant.notegrading.protocol;

/**
 * 技术专家领域：与技术指导 JSON 中 {@code domain} 字段一致（全大写枚举名）。
 */
public enum ExpertDomain {
    INTERNAL_NETWORK,
    DEV_GUIDE,
    EMPLOYEE_POLICY,
    GENERAL
}
